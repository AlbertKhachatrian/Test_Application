<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/container_view"
    android:layout_width="120dp"
    android:layout_height="120dp"
    android:layout_marginEnd="@dimen/margin_mid"
    xmlns:tools="http://schemas.android.com/tools"
    android:background="@drawable/bg_petrol"
    android:clickable="true"
    android:focusable="true">

        <ImageView
            android:id="@+id/petrol_image"
            android:layout_width="48dp"
            android:layout_height="48dp"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            android:layout_marginTop="@dimen/margin_small"
            android:background="@android:color/transparent"
            android:layout_marginStart="@dimen/margin_small"/>

        <com.google.android.material.textview.MaterialTextView
            android:id="@+id/petrol_price"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintBottom_toTopOf="@id/petrol_title"
            app:layout_constraintStart_toStartOf="parent"
            style="@style/Test.BodyText"
            android:textColor="@color/gray"
            android:layout_marginStart="@dimen/margin_small"
            tools:text="180$"/>

        <com.google.android.material.textview.MaterialTextView
            android:id="@+id/petrol_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/margin_small"
            android:layout_marginBottom="@dimen/margin_small"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            style="@style/Test.SubHeaderText"
            android:textColor="@color/black"
            tools:text="Regular"/>

    </androidx.constraintlayout.widget.ConstraintLayout>

